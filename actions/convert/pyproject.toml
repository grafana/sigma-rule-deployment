[project]
name = "convert"
version = "0.1.0"
description = "Sigma CLI Conversion"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dynaconf>=3.2.6",
    "pysigma-backend-loki>=0.12.2",
    "sigma-cli>=1.0.4",
]
