version: 2
folders:
  deployment_path: "./deployments"
defaults:
  integration:
    folder_id: abcdef123
    org_id: 23
  deployment:
    grafana_instance: https://myinstance.grafana.com
configurations:
  - name: group1_config
    integration:
      rule_group: "group1"
      time_window: "10m"
  - name: group2_config
    integration:
      rule_group: "group2"
      time_window: "1h"
  - name: group3_config
    integration:
      rule_group: "group3"
      time_window: "6h"
